<x-app-layout>
    <x-slot name="header"><h1 class="text-2xl font-bold text-gray-900 dark:text-white">Moderation queue</h1></x-slot>
    <div class="mx-auto max-w-5xl px-4 py-10 sm:px-6 lg:px-8">
        @if(session('success'))<div class="mb-6 rounded-lg bg-green-50 px-4 py-3 text-sm text-green-800">{{ session('success') }}</div>@endif
        <div class="space-y-5">@forelse($reports as $report)<article class="rounded-xl border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-700 dark:bg-gray-800"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-wide text-red-600">{{ $report->reason }}</p><a href="{{ route('prompts.show', $report->prompt) }}" class="mt-1 block text-lg font-bold text-gray-900 hover:text-indigo-600 dark:text-white">{{ $report->prompt->title }}</a><p class="mt-1 text-sm text-gray-500">Reported by {{ $report->reporter->name }} &middot; Prompt by {{ $report->prompt->user->name }}</p></div><div class="flex gap-2"><form method="POST" action="{{ route('moderation.dismiss', $report) }}">@csrf @method('PATCH')<button class="rounded-lg border border-gray-300 px-3 py-2 text-sm font-semibold dark:border-gray-600">Dismiss</button></form><form method="POST" action="{{ route('moderation.hide', $report) }}" onsubmit="return confirm('Hide this public prompt?')">@csrf @method('PATCH')<button class="rounded-lg bg-red-600 px-3 py-2 text-sm font-semibold text-white">Hide prompt</button></form></div></div>@if($report->details)<p class="mt-4 rounded-lg bg-gray-50 p-4 text-sm text-gray-700 dark:bg-gray-900 dark:text-gray-200">{{ $report->details }}</p>@endif</article>@empty<div class="rounded-xl border border-dashed border-gray-300 p-12 text-center dark:border-gray-700"><p class="font-semibold dark:text-white">The moderation queue is clear.</p></div>@endforelse</div><div class="mt-8">{{ $reports->links() }}</div>
    </div>
</x-app-layout>
